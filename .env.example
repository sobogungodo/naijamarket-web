# ============================================================================
# NAIJAMARKET INTEL - ENVIRONMENT CONFIGURATION
# ============================================================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control!
# ============================================================================

# =================================
# DATABASE
# =================================
# Azure SQL Database connection string
# Format: sqlserver://username:password@server.database.windows.net:1433;database=DatabaseName;encrypt=true;trustServerCertificate=false
DATABASE_URL="sqlserver://your-username:your-password@naijafood.database.windows.net:1433;database=NaijaMarketIntel;encrypt=true;trustServerCertificate=false"

# =================================
# DATABASE CREDENTIALS (canonical: AZURE_SQL_*, with DATABASE_*/SQL_* accepted as aliases)
# =================================
# The app reads discrete SQL-auth credentials (as opposed to the DATABASE_URL
# connection string above) via src/lib/env.ts -> env.db. See src/lib/env.README.md
# for the full precedence write-up. Set ONE convention below (not all three) —
# AZURE_SQL_* is preferred and is what new code/docs should use.
#
# Precedence (main app, src/): AZURE_SQL_* > DATABASE_* > SQL_* > built-in default.
#   Canonical:        AZURE_SQL_SERVER / AZURE_SQL_DATABASE / AZURE_SQL_USER / AZURE_SQL_PASSWORD
#   Alias (irregular DATABASE_NAME, not DATABASE_DATABASE):
#                      DATABASE_SERVER / DATABASE_NAME / DATABASE_USER / DATABASE_PASSWORD
#   Alias (irregular SQL_USERNAME, SQL_USER also accepted):
#                      SQL_SERVER / SQL_DATABASE / SQL_USERNAME (or SQL_USER) / SQL_PASSWORD
#
# NOTE: admin-dashboard/ is a SEPARATE app with its own lib/db.ts that uses an
# INVERTED SQL_*-first precedence (SQL_* wins over AZURE_SQL_* there). Don't assume
# the main app's precedence above applies to admin-dashboard.
AZURE_SQL_SERVER="naijafood.database.windows.net"
AZURE_SQL_DATABASE="naijafoodmarket-live"
AZURE_SQL_USER="your-sql-username"
AZURE_SQL_PASSWORD="your-sql-password"

# =================================
# DB BACKEND SWITCH (Supabase migration, dev-gated)
# =================================
# "supabase" routes DB reads through the Postgres/Supabase path instead of Azure SQL.
# Leave unset (defaults to Azure SQL) unless you are working on the Supabase migration.
DB_BACKEND="azure"
SUPABASE_DB_URL="postgresql://your-user:your-password@your-project.supabase.co:5432/postgres"

# =================================
# AUTHENTICATION
# =================================
# NextAuth.js configuration
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-super-secret-key-generate-with-openssl-rand-base64-32"

# Azure AD B2C (optional, for enterprise SSO)
# AZURE_AD_B2C_TENANT_NAME="your-tenant"
# AZURE_AD_B2C_CLIENT_ID="your-client-id"
# AZURE_AD_B2C_CLIENT_SECRET="your-client-secret"
# AZURE_AD_B2C_PRIMARY_USER_FLOW="B2C_1_signupsignin"

# =================================
# API KEYS
# =================================
# JWT signing secret
JWT_SECRET="your-jwt-secret-key-generate-with-openssl-rand-base64-64"

# Consumer-app JWT signing secret (separate from the trader/admin JWT_SECRET above)
CONSUMER_JWT_SECRET="your-consumer-jwt-secret-key-generate-with-openssl-rand-base64-64"

# Shared secret required on incoming Vercel Cron requests (Authorization: Bearer <value>)
CRON_SECRET="your-cron-secret-generate-with-openssl-rand-base64-32"

# API rate limiting
API_RATE_LIMIT_WINDOW_MS="60000"
API_RATE_LIMIT_MAX_REQUESTS="100"

# =================================
# EXTERNAL SERVICES
# =================================
# Twilio (for WhatsApp integration)
TWILIO_ACCOUNT_SID="your-twilio-account-sid-here"
TWILIO_AUTH_TOKEN="your-twilio-auth-token"
TWILIO_WHATSAPP_NUMBER="+14155238886"

# Paystack (for payments)
PAYSTACK_PUBLIC_KEY="pk_test_your-public-key"
PAYSTACK_SECRET_KEY="sk_test_your-secret-key"

# Flutterwave (alternative payment)
FLUTTERWAVE_PUBLIC_KEY="FLWPUBK_TEST-your-public-key"
FLUTTERWAVE_SECRET_KEY="FLWSECK_TEST-your-secret-key"

# =================================
# GOOGLE SHEETS (for data sync)
# =================================
GOOGLE_SHEETS_SPREADSHEET_ID="1n-7MXdoqvIoSHteBJaUYBmIPLjJBNtrE_jVuUxO5kr8"
# GOOGLE_SERVICE_ACCOUNT_EMAIL="your-service-account@project.iam.gserviceaccount.com"
# GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# =================================
# MONITORING & ANALYTICS
# =================================
# Azure Application Insights
# APPLICATIONINSIGHTS_CONNECTION_STRING="InstrumentationKey=your-key"

# Sentry (error tracking)
# SENTRY_DSN="https://your-key@sentry.io/project-id"

# =================================
# FEATURE FLAGS
# =================================
NEXT_PUBLIC_ENABLE_API_ACCESS="true"
NEXT_PUBLIC_ENABLE_REALTIME_PRICES="true"
NEXT_PUBLIC_ENABLE_PRICE_ALERTS="true"
NEXT_PUBLIC_ENABLE_EXPORT="true"

# =================================
# APPLICATION SETTINGS
# =================================
NEXT_PUBLIC_APP_NAME="NaijaMarket Intel"
NEXT_PUBLIC_APP_URL="https://naijamarket.intel"
NEXT_PUBLIC_SUPPORT_EMAIL="support@naijamarket.intel"

# Default timezone for price timestamps
NEXT_PUBLIC_DEFAULT_TIMEZONE="Africa/Lagos"

# Cache TTL in seconds
CACHE_TTL_PRICES="300"
CACHE_TTL_MARKETS="3600"
CACHE_TTL_ITEMS="3600"
